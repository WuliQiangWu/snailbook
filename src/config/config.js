// 配置项

// const host = 'http://localhost:5757'
const host = 'https://7yfyicmi.qcloud.la'

const config = {
  host: host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}
export default config
